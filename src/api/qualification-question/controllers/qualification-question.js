'use strict';

/**
 * qualification-question controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::qualification-question.qualification-question');
