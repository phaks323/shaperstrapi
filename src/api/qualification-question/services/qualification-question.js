'use strict';

/**
 * qualification-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::qualification-question.qualification-question');
