'use strict';

/**
 * work-history-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::work-history-question.work-history-question');
