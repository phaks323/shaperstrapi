'use strict';

/**
 * work-history-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::work-history-question.work-history-question');
